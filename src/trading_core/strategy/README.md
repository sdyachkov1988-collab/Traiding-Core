# Strategy Layer

This package is reserved for `Package B`.

Responsibilities:

- accept valid market/context input from the input layer;
- produce `StrategyIntent` or explicit no-action;
- avoid risk, execution, and accounting concerns.
