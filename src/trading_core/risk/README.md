# Risk Layer

This package is reserved for `Package C`.

Responsibilities:

- evaluate upstream strategy-side intent;
- return a separate `RiskDecision`;
- avoid direct order submission or state mutation ownership.
