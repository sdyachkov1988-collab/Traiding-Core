# Tests

Planned test categories:

- contract tests for interface seams;
- invariant tests from the guardrails document;
- execution boundary tests with mock adapters;
- end-to-end acceptance slice tests for `Minimal Core v1`.
